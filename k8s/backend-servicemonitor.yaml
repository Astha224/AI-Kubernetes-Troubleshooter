apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: ai-backend-monitor
  namespace: monitoring

spec:
  selector:
    matchLabels:
      app: ai-backend

  namespaceSelector:
    matchNames:
      - default

  endpoints:
    - port: http
      path: /metrics
      interval: 15s